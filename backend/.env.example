AWS_REGION=
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
ROOT_BUCKET_NAME=
AUTH_USER=
AUTH_PASS=
JWT_SECRET=
PUBLIC_URL=
PUBLIC_DOMAIN=
PORT=

# Index Worker (optional)
# Path to SQLite database for tracking index jobs
INDEX_DB_PATH=./data/index.db
# Number of files to process per batch
INDEX_BATCH_SIZE=10
# Max retry attempts for failed indexing operations
INDEX_MAX_RETRIES=3
# Milliseconds between polling for new files to index
INDEX_POLL_INTERVAL=2000
# Base backoff time in ms for rate limiting (doubles on each retry)
INDEX_BACKOFF_MS=5000